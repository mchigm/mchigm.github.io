{
  "name": "universal-preview",
  "displayName": "Universal File Preview",
  "description": "Preview Markdown, PDF, Office, TeX, and Web files locally with live edit support.",
  "version": "0.0.1",
  "publisher": "mchigm",
  "repository": {
    "type": "git",
    "url": "https://github.com/mchigm/mchigm.github.io"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "universalPreview.open",
        "title": "Universal Preview: Open Preview",
        "icon": "$(preview)"
      }
    ],
    "menus": {
      "editor/title/run": [
        {
          "command": "universalPreview.open",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "universalPreview.open",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Universal Preview",
      "properties": {
        "universalPreview.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Enable real-time preview refresh when editing."
        },
        "universalPreview.refreshDelay": {
          "type": "number",
          "default": 300,
          "description": "Delay in milliseconds before refreshing the preview."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^16.x",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "mammoth": "^1.11.0",
    "markdown-it": "^13.0.1",
    "xlsx": "^0.18.5"
  }
}
